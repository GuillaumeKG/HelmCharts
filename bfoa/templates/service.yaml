apiVersion: v1
kind: Service
metadata:
  name: {{ include "bfoa.fullname" . }}
  namespace: {{ include "bfoa.namespace" . }}
  labels:
{{ include "bfoa.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bfoa.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
