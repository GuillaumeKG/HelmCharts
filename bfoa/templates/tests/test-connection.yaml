
apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Values.nameOverride }}-test-connection"
  labels:
{{ include "bfoa.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ .nameOverride }}:{{ .Values.service.port }}']
  restartPolicy: Never
